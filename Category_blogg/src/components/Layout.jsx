export default function Layout({children}){
    return(
        <>
        <header>
            <nav>
                <ul>
                    <li>Kommer link..</li>
                </ul>
            </nav>
        </header>
        {children}
        <footer>Footer</footer>
        </>
    )
}